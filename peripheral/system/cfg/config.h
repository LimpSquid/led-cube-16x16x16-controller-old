#ifndef CONFIG_H
#define	CONFIG_H

#include "../../../lib/std/stdtypes.h"

/**
 * Low level CPU initialization to get the CPU up and running
 * @return Returns 'true' on success, else 'false'
 */
bool config_cpu_init();

#endif	/* CONFIG_H */

